# BarberPro — SDD Configuration
# Auto-generated by sdd-init on 2026-05-13

project: BarberPro
stack:
  backend:
    language: Python 3.12
    framework: FastAPI
    database: PostgreSQL
    cache: Redis
    orm: SQLAlchemy
    migrations: Alembic
    package_manager: UV
    automations: n8n (webhook-based)
  frontend:
    framework: React 18
    build_tool: Vite
    styling: Tailwind CSS
    package_manager: npm/pnpm
    api_client: fetch/axios

paths:
  backend: /home/marcos/Documentos/BarberPro/backend
  frontend: /home/marcos/Documentos/BarberPro/frontend

persistence:
  mode: hybrid  # Engram + openspec/

delivery:
  strategy: auto-chain
  chain_strategy: feature-branch-chain

strict_tdd: false

testing:
  runner:
    available: false
    command: N/A
    framework: N/A
  layers:
    unit: { available: false, tool: "—" }
    integration: { available: false, tool: "—" }
    e2e: { available: false, tool: "—" }
  coverage:
    available: false
    command: "—"
  quality:
    linter: { available: false, command: "—" }
    type_checker: { available: false, command: "—" }
    formatter: { available: false, command: "—" }

phases:
  explore: {}
  propose: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
